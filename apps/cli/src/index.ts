// =============================================================================
// ultracontext — public entry. Re-exports the SDK so `import 'ultracontext'`
// gives the typed HTTP client. The `uc` binary lives in src/cli/.
// =============================================================================

export * from '@ultracontext/js';
