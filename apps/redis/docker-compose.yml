services:
  redis:
    image: redis:7-alpine
    container_name: ultracontext-redis
    command: redis-server --appendonly yes --save 60 1000
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
