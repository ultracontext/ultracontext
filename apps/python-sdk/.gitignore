# bundled platform `uc` binaries — built into wheels by CI, never committed
ultracontext/_bin/

# local virtualenv + build artifacts
.venv/
dist/
build/
*.egg-info/

# local sqlite stores created by the local backend / tests
*.db
*.db-*
