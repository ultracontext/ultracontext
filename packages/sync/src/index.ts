// =============================================================================
// @ultracontext/sync — fs-first Mutagen sync orchestration
// =============================================================================

// Public surface is filled in during the Sync phase. Stub keeps the package a
// valid ESM module so the workspace installs + typechecks today.

export {};
