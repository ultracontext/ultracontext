# @ultracontext/protocol

Internal workspace package with shared daemon/TUI protocol helpers:

- Local daemon WebSocket host/port/info-file resolution
- WebSocket message type constants + parse/build helpers
- Shared bootstrap state key builder
- Bootstrap mode normalization
- Safe JSON parsing helpers
