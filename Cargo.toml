# workspace root: reusable Rust implementation under core/, product surfaces at top level
[workspace]
members = [
    "core/engine",
    "cli",
    "core/bindings-js",
    "core/bindings-python",
]
resolver = "3"

[profile.release]
lto = true
strip = true
