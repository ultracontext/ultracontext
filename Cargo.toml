# workspace root: components at top level, languages inside sdks/
[workspace]
members = ["core", "cli", "sdks/js/native", "sdks/python/native"]
resolver = "3"

[profile.release]
lto = true
strip = true
