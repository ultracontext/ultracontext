# workspace root: Rust implementation under core/, user-facing packages under sdks/
[workspace]
members = [
    "core/engine",
    "core/cli",
    "core/bindings-js",
    "core/bindings-python",
]
resolver = "3"

[profile.release]
lto = true
strip = true
